// 더미/시드 데이터 제거됨 (보안).
// 실제 계정·데이터는 앱의 "계정 관리" UI를 통해 Firestore에 직접 등록·관리한다.
// mock 모드가 참조하는 export 이름은 유지하되 내용은 비워, 하드코딩된
// 비밀번호·실명·전화번호가 코드에 남지 않도록 한다.
// (빈 배열이므로 Seed 기능을 실행해도 생성되는 계정/데이터가 없다.)

export const demoUsers = [];
export const initialTeams = [];
export const initialZones = [];
export const initialMembers = [];
export const initialMonths = [];
export const initialAttendanceRecords = [];
export const initialMonthlyAchievements = [];
export const initialAuditLogs = [];
